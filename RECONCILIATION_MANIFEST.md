# Reconciliation Manifest — swift_store/swift_store Donor Tree

Generated by forensic pass against `master` @ `f3269e3928eee3838b2d50294e47e6c0d47e2668`
("fix: enforce merchant entitlements and promotion idempotency", committed by Thabo, 2026-09-11).

## What this zip is

The top-level canonical tree, **unchanged except** for the 22 dependency-clean files
listed under HARVESTED below. Nothing in Tier 0 (financial/inventory/reservation) or
Tier 1 (delivery, W2.4-frozen) was touched. Nothing in the 90 diverging files was
overwritten. The orphaned `swift_store/swift_store/` donor directory has been removed
from this output — it remains in your working copy as the harvest source.

**Toolchain caveat:** this manifest is backed by static (grep-level) dependency
verification only. No Android SDK / Gradle / `google()` maven access exists in this
sandbox, so `:app:compileDevDebugKotlin` was NOT run against this tree. The
`functions/` (TypeScript) side was not modified, so no new `tsc` run was needed.
**Final compile verification still has to happen in your terminal-relay workflow
before this is committed.**

---

## HARVESTED (22 files) — copied from nested tree, dependency-clean by static check

```
app/src/test/java/com/swiftshop/GeospatialContractTest.kt
app/src/test/java/com/swiftshop/W25B_UIContractTest.kt
core/media/src/main/java/com/swiftshop/core/media/ReelUploadManager.kt
core/media/src/test/java/com/swiftshop/core/media/ReelUploadManagerTest.kt
core/model/src/main/java/com/swiftshop/core/model/MarketContext.kt
core/model/src/test/java/com/swiftshop/core/model/Phase1ContractTest.kt
core/ui/src/main/java/com/swiftshop/core/ui/components/CommentComponents.kt
core/ui/src/main/java/com/swiftshop/core/ui/components/ComposeBitmapUtil.kt
core/ui/src/main/java/com/swiftshop/core/ui/components/MapInitializer.kt
core/ui/src/main/java/com/swiftshop/core/ui/components/VideoPlayer.kt
data/firebase/src/main/java/com/swiftshop/data/firebase/FirebaseContextEngine.kt
data/firebase/src/main/java/com/swiftshop/data/firebase/FirebaseUserPreferenceRepository.kt
data/firebase/src/main/java/com/swiftshop/data/firebase/Mappers.kt
data/firebase/src/test/java/com/swiftshop/data/firebase/ArchitectureMapperTest.kt
data/firebase/src/test/java/com/swiftshop/data/firebase/WorkflowFoundationTest.kt
domain/commerce/src/main/java/com/swiftshop/domain/commerce/ContextEngine.kt
domain/commerce/src/main/java/com/swiftshop/domain/commerce/DiscoverShopsUseCase.kt
domain/commerce/src/test/java/com/swiftshop/domain/commerce/CreateListingUseCaseTest.kt
domain/commerce/src/test/java/com/swiftshop/domain/commerce/MerchantQuotaTest.kt
domain/profile/src/main/java/com/swiftshop/domain/profile/UserPreferenceRepository.kt
feature/profile/src/main/java/com/swiftshop/feature/profile/BookmarksScreen.kt
feature/shop/src/main/java/com/swiftshop/feature/shop/CreateListingGatewayScreen.kt
```

**Residual risk on these 22 files:** static grep-level check confirms no reference to
Tier 0/Tier 1 restricted symbols, and no unresolved `com.swiftshop.*` import. It does
NOT confirm: (a) Gradle module dependency declarations are complete for any new
library usage, (b) same-package symbol references that don't require an explicit
import, (c) resource references (drawables/strings) the files may expect. Real proof
is the Gradle build in Android Studio.

Two of these (`ContextEngine.kt` domain-side + `FirebaseContextEngine.kt`) are a
matched pair — `WorkflowContext` is declared in the former and consumed by the
latter. Harvested together deliberately; don't split them across separate commits.

---

## BLOCKED — Tier 2 candidates, NOT harvested (depend on unreconciled diverging files)

| File | Blocked by |
|---|---|
| `core/ui/.../GlowModifiers.kt` | needs `MaterialTheme.glow` — only in nested `Theme.kt` (diverging) |
| `core/ui/.../MapProfilePin.kt` | needs `MaterialTheme.glow` — only in nested `Theme.kt` (diverging) |
| `core/ui/.../SwiftMapView.kt` | needs `MapMarker`/`MapPolyline`/`SwiftEntity` — only in nested `Models.kt` (diverging) |
| `domain/commerce/.../WorkflowMapping.kt` | needs `OrderType`/`FulfillmentType` — only in nested `Models.kt` (diverging) |
| `data/firebase/.../FirebaseCommentRepository.kt` | needs `CommentRepository` iface — only in nested `FeedEngine.kt` (diverging) |
| `feature/reels/.../ReelDetailScreen.kt` | needs `GetPostUseCase` — only in nested `FeedEngine.kt` (diverging) |

**Unlocks when:** `core/model/Models.kt`, `core/ui/theme/Theme.kt`, and
`domain/feed/FeedEngine.kt` go through their own diverging-file reconciliation
(Stage 3). Re-run the harvest for these six once those three land.

## BLOCKED — Tier 0/Tier 1, not eligible for harvest under current governance

| File | Reason |
|---|---|
| `feature/shop/.../EditListingScreen.kt` | depends on `UpdateListingUseCase` (Tier 0 — inventory mutation) |
| `feature/shop/.../EditListingViewModel.kt` | depends on `UpdateListingUseCase` (Tier 0 — inventory mutation) |
| `domain/commerce/AvailabilityRepository.kt`, `InitiateBookingUseCase.kt`, `ObserveAvailableSlotsUseCase.kt`, `UpdateListingUseCase.kt` | Tier 0 — reservation/inventory logic, needs dedicated audit |
| `data/firebase/FirebaseAvailabilityRepository.kt` | Tier 0, same as above |
| `feature/profile/components/WalletCard.kt` | wallet-adjacent, held with Tier 0 out of caution |
| `feature/delivery/DriverLocationPublisher.kt`, `RequestDeliveryScreen.kt`, `RequestDeliveryViewModel.kt` | Tier 1 — W2.4 frozen |
| `domain/delivery/GeoProvider.kt` | Tier 1 — W2.4 frozen |
| `feature/orders/TrackOrderScreen.kt` | Tier 1 — W2.4 frozen |

All 90 files in the **diverging** set (functions/src/commerce.ts, finance.ts, mopay.ts,
Models.kt, Theme.kt, FeedEngine.kt, every feature UI/ViewModel pair, gradle configs,
etc.) are **untouched in this zip** — canonical top-level content preserved exactly as
committed at `f3269e3`, pending Stage 3 per-file reconciliation.

---

## UNCLASSIFIED — not included, pending your call

- `cart_view.xml`, `cart_view_real.xml`, `home_view.xml`, `view.xml` — confirmed
  `uiautomator dump` accessibility-tree snapshots (not Android resources). Zero
  references anywhere in the repo. `home_view.xml`/`view.xml` show the actual dev
  build's home feed rendering with real listing data ("Sprinter van", "Bruder
  caravan", "Verified Test Product..."); `cart_view.xml`/`cart_view_real.xml` show
  unrelated OS/launcher screens, likely captured by accident. Recommend
  `DELETE-AS-ARTIFACT` once you confirm.
- Debug scripts (`find_incident_fixed.js`, `find_order_by_session.js`,
  `find_order_direct.js`, `verify_orders.js`, `scripts/W2_3_test_runner.js`,
  `scripts/inspect_delivery_listings.js`, `scripts/verify_W2_3.js`) — not copied in.
  Per your note, these need inspection for target Firebase project, schema
  assumptions (old `stockQuantity` model?), and whether they assume the M25
  hardcoded fee, before being trusted as evidence.
- `DELIVERY_LISTING_ARCHITECTURE.md`, `docs/inventory_deployment_readiness_audit.md`,
  `docs/inventory_transition_implementation_plan.md` — reference docs, not copied in,
  recommend reading before Tier 0/Tier 1 audits since they likely describe intent
  behind the blocked files above.

---

## Next steps (proposed, unauthorized — awaiting your sign-off)

1. Validate this zip's 22 harvested files against a real Gradle build in Android
   Studio (I can't run that here).
2. `commerce.ts`/`finance.ts`/`mopay.ts` dedicated audit — diff already scoped from
   prior forensic pass (hardcoded M25 fee at lines 30/129 of canonical
   `commerce.ts` vs. the listing-price-aware nested version).
3. Reconcile `Models.kt`, `Theme.kt`, `FeedEngine.kt` (unblocks the 6 held-back Tier 2
   files above).
4. Your decision on the W2.4 freeze before Tier 1 is touched at all.
5. Classification call on the 4 XML files and the debug scripts.
